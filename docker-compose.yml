version: '3.8'

services:
  web:
    image: baby-photo-uploade:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8182:80"
    volumes:
      - ./src:/var/www/html  
      - ./uploads:/var/www/html/uploads
    environment:
      - MAIL_TO=stukerr@hotmail.com
